import { FilterByCicloPipe } from './filter-by-ciclo.pipe';

describe('FilterByCicloPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCicloPipe();
    expect(pipe).toBeTruthy();
  });
});
